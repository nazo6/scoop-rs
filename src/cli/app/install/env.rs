use interface::{bucket_app::BucketApp, manifest::Manifest};

pub async fn path(app: &BucketApp<'_>, manifest: &Manifest) -> anyhow::Result<()> {
    todo!()
}

pub async fn set_env(app: &BucketApp<'_>, manifest: &Manifest) -> anyhow::Result<()> {
    todo!()
}
