use interface::{bucket_app::BucketApp, manifest::Manifest};

pub async fn create_shims(app: &BucketApp<'_>, manifest: &Manifest) -> anyhow::Result<()> {
    todo!()
}

pub async fn create_startmenu_shortcuts(
    app: &BucketApp<'_>,
    manifest: &Manifest,
) -> anyhow::Result<()> {
    todo!()
}
